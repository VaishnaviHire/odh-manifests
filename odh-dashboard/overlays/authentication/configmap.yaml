# Enables the Jupyter Spawner UI for the notebook-controller
kind: ConfigMap
apiVersion: v1
metadata:
  name: odh-dashboard-overlay-authentication-deprecated
  labels:
    opendatahub.io/configurable: "true"
data:
  MESSAGE: "YOU CAN SAFELY REMOVE THE AUTHENTICATION OVERLAY FROM THE ODH-DASHBOARD KUSTOMIZECONFIG IN THE KFDEF"
